# Hello Provenance

This demo executes the canonical secure flow:

1. verify signed bundle
2. run with explicit policy
3. verify emitted receipt hash

Run:

```bash
./demos/hello-provenance/run.sh
```

Output receipt is written to `artifacts/hello-provenance/receipt.json`.
